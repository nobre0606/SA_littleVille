# Copie para .env (o .env real nunca entra no git — ver .gitignore).
# true = usa o mock (latência simulada, sem back-end no ar). Qualquer outro valor: chama as
# rotas reais da Fase 5 em VITE_API_URL.
VITE_USE_MOCK=true
VITE_API_URL=http://localhost:3001/api
