/** Placeholder — o mapa de verdade é outra Fase (fora do escopo deste brief). */
export default function Mapa() {
  return (
    <main className="flex min-h-dvh items-center justify-center bg-[#05080F] text-[#EAF6FF]">
      <p>Mapa da vila (placeholder) — aqui entra a Fase do mapa.</p>
    </main>
  )
}
